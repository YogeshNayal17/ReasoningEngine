<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <FrameLayout
        android:id="@+id/selection_canvas_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="#CC000000"
        android:padding="16dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/selection_instruction"
            android:textColor="#FFFFFF"
            android:textSize="16sp" />

        <Button
            android:id="@+id/selection_cancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/selection_cancel"
            android:background="@android:color/transparent"
            android:textColor="#FFFFFF" />

    </LinearLayout>

    <ImageButton
        android:id="@+id/selection_confirm"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="bottom|end"
        android:layout_margin="24dp"
        android:background="@drawable/bubble_background"
        android:src="@drawable/ic_check"
        android:contentDescription="@string/selection_confirm_description" />

</FrameLayout>
